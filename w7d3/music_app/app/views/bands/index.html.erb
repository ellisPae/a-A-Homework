<h1 class="index-header"> Skynet's Personal Artist Library </h1>

<ul class="artist-list">
    <% @bands.each do |band| %>
        <li>
            <a href="<%= band_url(band) %>">
                <%= band.name %>
            </a>    
        </li>
    <% end %>
</ul>




